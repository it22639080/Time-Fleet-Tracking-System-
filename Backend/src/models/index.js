const LocationHistory = require('./location-history.model');
const Alert = require('./alert.model');
const GeoFence = require('./geo-fence.model');
const LocationPoint = require('./location-point.model');
const Trip = require('./trip.model');
const User = require('./user.model');
const Vehicle = require('./vehicle.model');

module.exports = {
  Alert,
  GeoFence,
  LocationHistory,
  LocationPoint,
  Trip,
  User,
  Vehicle
};
